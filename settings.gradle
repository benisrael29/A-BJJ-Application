pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
/**
 * Fragments require a dependency on the AndroidX Fragment library. You need to add the Google Maven
 * repository to your project's settings.gradle file in order to include this dependency.
 */
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "My BJJ Application"
include ':app'
